import * as React from 'react'

import './component.less'

const SignedIn: React.StatelessComponent<{}> = (): JSX.Element => (
	<section id='signed-in'>
		<h1>Signed in</h1>
	</section>
)

export default SignedIn
