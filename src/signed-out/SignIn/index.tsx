import * as React from 'react'

import './component.less'

const SignIn: React.StatelessComponent<{}> = (): JSX.Element => (
	<section id='sign-in'>
		<h2>Sign in</h2>
	</section>
)

export default SignIn
